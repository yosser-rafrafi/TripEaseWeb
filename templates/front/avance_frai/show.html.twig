<!DOCTYPE html>

<title>AvanceFrai</title>

{% block body %}
    <h1>AvanceFrai</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avance_frai.id }}</td>
            </tr>
            <tr>
                <th>Employe_id</th>
                <td>{{ avance_frai.employeId }}</td>
            </tr>
            <tr>
                <th>Voyage_id</th>
                <td>{{ avance_frai.voyageId }}</td>
            </tr>
            <tr>
                <th>Montant_demande</th>
                <td>{{ avance_frai.montantDemande }}</td>
            </tr>
            <tr>
                <th>Montant_accorde</th>
                <td>{{ avance_frai.montantAccorde }}</td>
            </tr>
            <tr>
                <th>Devise</th>
                <td>{{ avance_frai.devise }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ avance_frai.motif }}</td>
            </tr>
            <tr>
                <th>Type_avance</th>
                <td>{{ avance_frai.typeAvance }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ avance_frai.statut }}</td>
            </tr>
            <tr>
                <th>Date_demande</th>
                <td>{{ avance_frai.dateDemande ? avance_frai.dateDemande|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_validation</th>
                <td>{{ avance_frai.dateValidation ? avance_frai.dateValidation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Commentaire_manager</th>
                <td>{{ avance_frai.commentaireManager }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avance_frai_index') }}">back to list</a>

    <a href="{{ path('app_avance_frai_edit', {'id': avance_frai.id}) }}">edit</a>

    {{ include('front/avance_frai/_delete_form.html.twig') }}
{% endblock %}
