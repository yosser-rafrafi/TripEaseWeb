<form method="post" action="{{ path('app_frai_delete', {'id': frai.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frai.id) }}">
    <button class="btn">Delete</button>
</form>
