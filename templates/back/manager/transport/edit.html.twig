{% extends 'back/base.html.twig' %}

{% block title %}Edit Transport{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h5><a href="{{ path('app_transport_index') }}" class="btn btn-outline-secondary">← Back to Transport List</a></h5>
        </div>

        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card shadow-lg border-0 rounded-3">
                    <div class="card-header bg-primary text-white text-center">
                        <h2 class="mb-0">Edit Transport</h2>
                    </div>
                    <div class="card-body p-4">
                        {{ include('back/manager/transport/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
